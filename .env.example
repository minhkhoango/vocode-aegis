# .env
# Environment variables for the Docker Compose services

# Redis configuration (used by the backend service)
REDIS_HOST=redis
REDIS_PORT=6379

# Dashboard application configuration
DASHBOARD_PORT=3001 # Port on your host machine to access the dashboard
DASHBOARD_REFRESH_INTERVAL=5000 # Data refresh interval in milliseconds (e.g., 5000 = 5 seconds)
LOG_LEVEL=INFO # Logging level for the FastAPI backend (DEBUG, INFO, WARNING, ERROR, CRITICAL)

# Frontend build settings (injected into React build process)
# REACT_APP_WS_URL and REACT_APP_API_URL should match your DASHBOARD_PORT
# Note: These are for the *browser* to connect to, so they use localhost on your host machine's port.
REACT_APP_WS_URL=ws://localhost:3001/ws
REACT_APP_API_URL=http://localhost:3001

# Optional: If Vocode's Redis is remote or on a different network, you'd adjust REDIS_HOST/REDIS_PORT.
# Example:
# REDIS_HOST=your_vocode_redis_external_ip
# REDIS_PORT=6379
